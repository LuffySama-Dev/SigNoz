apiVersion: v1
kind: Pod
metadata:
  name: ppending-test
  namespace: default
spec:
  containers:
  - name: busybox
    image: busybox
    command: ["sleep","3600"]
  nodeSelector:
    non-existent: "true"

